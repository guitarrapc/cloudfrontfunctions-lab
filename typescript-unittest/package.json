{
  "name": "typescript-unittest",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test test/*.test.js"
  },
  "devDependencies": {
    "@types/aws-cloudfront-function": "^1.0.8",
    "typescript": "^7.0.2"
  }
}
